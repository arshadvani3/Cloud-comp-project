apiVersion: v1
kind: Service
metadata:
  name: llm-inference-service
  namespace: llm-inference
spec:
  type: LoadBalancer
  selector:
    app: llm-inference
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
